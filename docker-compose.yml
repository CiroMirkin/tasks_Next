version: '3'

services:
  tasksDB:
    image: postgres:15.3
    container_name: tasks-db
    restart: always
    ports:
      - 5432:5432
    enviroment:
      - POSRGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    volumes:
      -./postgres:/var/lib/postgresql/datacl